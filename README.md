# miRNA-lung-cancer-stage-prediction
## Set up environment
### Requirments
- Please install all the required modules with:
```
pip install -r requirements.txt
```
- CUDA 12.x is required for XGBoost to run on gpu devices
